# 2026.250.1019 — feat: pin the crabbox: verb surface to CLI v0.51.0

## Summary

Aligns the `crabbox:` check verb with the released **CLI v0.51.0** (upstream openclaw/crabbox — carries the #1924 no-hostname implementation): candy `version:`/`pluginCalVer` → `2026.250.0100`, the dispatch check asserts `0.51.0`, and the `leases` argv mapping was RE-VERIFIED live against the v0.51.0 binary (R1 — `crabbox list` etc. all recognized commands).

## How tested

Fresh-rebuild bed run on the `disposable: true` target `check-crabbox-pod` (repo overrides feed these PR branches into the build):

```
EVAL BED: check-crabbox-pod — exit 0 (13/13 steps)
PASS [cbx-cli-binary]      ok
PASS [cbx-verb-version]    0.51.0          <- in-venue crabbox --version via the verb
PASS [crabbox-verb-dispatches]  0.51.0     <- the plugin's dispatch check (this PR's asserted value) through the provider registry
PASS [cbx-verb-providers]  local-container matrix present
PASS [cbx-verb-config]     config=/home/user/.config/crabbox/config.yaml
PASS [cbx-verb-doctor]     ok
```

The e2e bed also exercised the broker-backed verb methods (usage/leases) + the full local loop with the v0.51.0 CLI (see pod-crabbox#3). Plugin gates: `go build ./...` and `go test ./...` pass.

## Rulebook compliance
- R1: argv mappings verified live against the pinned binary, not assumed; stale 0.48.x comment fixed.
- R7: bed output pasted above (dispatch check green at 0.51.0).
- R10: fresh-rebuild disposable proof on the final tree.
- R2/R3/R4/R5/R6/R8/R9: N/A or satisfied — surface alignment only; no parallel vocab; branch-per-change PR.

## Change classification

- **Class:** plugin version alignment (contract consumer). **Verification gate:** fresh-rebuild disposable bed (exit 0) + live binary probe. **Attribution:** fully tested and validated.

*Assisted-by: pi openrouter/deepseek/deepseek-v4-flash-0731 (fully tested and validated)*